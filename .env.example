# Copy this file to .env and fill in real values. Do NOT commit .env.
# These configure the postgres container (docker-compose.yml).
POSTGRES_USER=postgres
POSTGRES_PASSWORD=change-me
POSTGRES_DB=DoorsWeb
