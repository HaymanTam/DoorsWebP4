persistence true
persistence_location /mosquitto/data/
log_dest stdout

listener 1883
# Dev-only: open access. Set to false and add a password_file before any non-local deployment.
allow_anonymous true

# WebSocket listener (e.g. for browser MQTT clients)
listener 9001
protocol websockets
